name: GitHub Actions
on:
  push:
    branches:
      - dev
  pull_request:
    branches:
      - dev

jobs:
  build:
    name: Build Job
    runs-on: ubuntu-latest
    steps:
      - run: echo "Running build job"
  test:
    name: Test Job
    needs: build
    runs-on: ubuntu-latest
    steps:
      - run: echo "Running test job"
